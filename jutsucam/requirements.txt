# Pinned to 0.10.14 deliberately: this is the last mediapipe release whose
# Python API ships the classic `mp.solutions.hands` module with its
# pretrained model bundled *inside* the pip package. Newer 0.10.2x/0.10.3x
# releases removed `solutions` in favour of a "Tasks" API that downloads
# its model file from Google Cloud Storage at first run -- an extra
# runtime dependency and failure point we deliberately avoid here so the
# project works fully offline immediately after `pip install`.
mediapipe==0.10.14
opencv-python>=4.9,<5
numpy>=1.26,<2.6
matplotlib>=3.7
pytest>=7.4
